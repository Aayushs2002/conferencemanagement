# Additional .env Configuration (Optional)

# Add these to your .env file if you want to make batch size configurable

# Workshop pass generation settings
WORKSHOP_PASS_BATCH_SIZE=50
WORKSHOP_PASS_MEMORY_LIMIT=512M
WORKSHOP_PASS_MAX_EXECUTION_TIME=300
WORKSHOP_PASS_BATCH_THRESHOLD=100

# Usage example in controller:
# $perPage = config('workshop.pass_batch_size', 50);
# ini_set('memory_limit', config('workshop.pass_memory_limit', '512M'));

# Note: To use these, you would need to:
# 1. Create a config file: config/workshop.php
# 2. Add the configuration keys
# 3. Update the controller to use config() instead of hardcoded values

# For now, the values are hardcoded in the controller for simplicity.
# This file is for future reference if you want to make it configurable.
